import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iqbalwoodcraft.app',
  appName: 'IQBAL WOODCRAFT',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
